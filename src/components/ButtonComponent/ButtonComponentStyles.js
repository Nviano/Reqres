import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    button: {
        color: '#fff',
        textTransform: 'uppercase',
        border: 'none',
        marginTop: 30,
        fontSize: 16,
        fontWeight: 900,
        letterSpacing: 5,
        display: 'block',
        appearance: 'none',
        borderRadius: 4,
    }
}));